// render a tree browser for the project's files.
// closing and reopening a folder should reload the contents
//    file watching should be supported but that's for later
// the "project(s)" is stored in the tree, rather than being per-window.

pub fn init() void {}
pub fn deinit() void {}
pub fn render() void {}

/// for saving and reloading workspaces
pub fn serialize() void {}
pub fn deserialize() void {}
